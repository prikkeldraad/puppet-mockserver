[Unit]
Description=Mockserver
After=network.target

[Service]
User=<%= @user %>
ExecStart=<%= @dir %>/<%= @version %>/bin/mockserver.sh start
ExecStop=<%= @dir %>/<%= @version %>/bin/mockserver.sh stop

[Install]
WantedBy=multi-user.target
