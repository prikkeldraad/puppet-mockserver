[Unit]
    Description=wrapper for all Mockserver instances
    After=network.target

[Service]
    Type=oneshot
    ExecStart=/bin/true
    RemainAfterExit=yes

[Install]
    WantedBy=multi-user.target
